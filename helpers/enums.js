const slotStatus = {
    OPEN : 'open',
    BOOKED: 'booked',
    DISABLED: 'disabled'
}

const appointmentStatus = {
    BOOKED : 'booked',
    CANCELLED: 'cancelled',
    COMPLETED: 'completed'
}

module.exports = {slotStatus, appointmentStatus}