const slotStatus = {
    OPEN : 'open',
    DISABLED: 'disabled'
}

const appointmentStatus = {
    BOOKED : 'booked',
    CANCELLED: 'cancelled',
    COMPLETED: 'completed'
}

module.exports = {slotStatus, appointmentStatus}